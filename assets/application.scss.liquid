/*
 * RestoReady v1.0
 * Copyright 2014 MH Communication.
 * Author MH Communication @MHCommunication
 * Built with Sass - http://sass-lang.com/

 * Some things to know about this file:
    - Sass is compiled on RestoReady's server so you don't need to convert it to CSS yourself
    - The output CSS is compressed and comments are removed
    - You cannot use @imports in this file
    - Helpers variables, mixins, and starter classes are provided. Change as needed.
    - The file is prepped with a CSS reset
*/

/* Table of Contents
==============================================================================
  Colors
  Box Model
  Breakpoint and Grid Variables
  General Variables
  Sass Mixins
  Dependency-free breakpoint mixin
  Reset
  Grid Setup
  Basic Styles
  Helper classes
  Typography
  Lists
  Images and Iframes
  Forms
  Links and buttons
  Layout
  Modules
  Icomoon Font
  Breakpoints
=========================================================================== */

// ===========================================================================
// #Colors
// ===========================================================================
$body-background: {{ theme_settings.body-background }};
$body-color: {{ theme_settings.body-color }};
$accent-color: {{ theme_settings.accent-color }};
$font-color-on-accent-background: {{ theme_settings.font-color-on-accent-background }};

$nav-background: {{ theme_settings.nav-background }};
$nav-font-color: {{ theme_settings.nav-font-color }};

$card-background: {{ theme_settings.card-background }};
$card-font-color: {{ theme_settings.card-font-color }};
$card-background-hover: {{ theme_settings.card-background-hover }};
$summary-color: {{  theme_settings.summary-color }};
$food-price-value-color: {{ theme_settings.food-price-value-color }};

$tag-vegetarian-background: {{ theme_settings.tag-vegetarian-background }};
$tag-no-meat-background: {{ theme_settings.tag-no-meat-background }};
$tag-news-background: {{ theme_settings.tag-news-background }};
$tag-no-pork-background: {{ theme_settings.tag-no-pork-background }};
$tag-no-alcohol-background: {{ theme_settings.tag-no-alcohol-background }};
$tag-daily-food-background: {{ theme_settings.tag-daily-food-background }};

$tag-vegetarian-font-color: {{ theme_settings.tag-vegetarian-font-color }};
$tag-no-meat-font-color: {{ theme_settings.tag-no-meat-font-color }};
$tag-news-font-color: {{ theme_settings.tag-news-font-color }};
$tag-no-pork-font-color: {{ theme_settings.tag-no-pork-font-color }};
$tag-no-alcohol-font-color: {{ theme_settings.tag-no-alcohol-font-color }};
$tag-daily-food-font-color: {{ theme_settings.tag-daily-food-font-color }};

$font-color-on-photo: {{ theme_settings.font-color-on-photo }};

// ===========================================================================
// #Box model
// ===========================================================================
*, input, :before, :after {
  -webkit-box-sizing:border-box;
  -moz-box-sizing:border-box;
  box-sizing:border-box;
}

// ===========================================================================
// #Breakpoint and Grid Variables
// ===========================================================================
$viewportIncrement: 1px;

$small: 480px;
$medium: 768px;
$large: 769px;

$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

/* The following are dependencies of csswizardry grid */
$breakpoints: (
  'small' '(max-width: #{$small})',
  'medium' '(min-width: #{$postSmall}) and (max-width: #{$medium})',
  'medium-down' '(max-width: #{$medium})',
  'large' '(min-width: #{$large})'
);
$breakpoint-has-widths: ('small', 'medium', 'medium-down', 'large');
$breakpoint-has-push:  ('medium', 'medium-down', 'large');
$breakpoint-has-pull:  ('medium', 'medium-down', 'large');

// ===========================================================================
// #General Variables
// ===========================================================================


/* Width variables */
$siteWidth: 960px;
$gutter: 24px;
$gridGutter: 24px; // can be a %, but nested grids will have smaller margins because of it

/* Radius */
$radius: .25em;

/* Typography */
$headerFontStack: {{ theme_settings.header-font }};
$bodyFontStack: {{ theme_settings.body-font }};



// ===========================================================================
// #Sass Mixins
// ===========================================================================
.clearfix {
  &:after {
    content: "";
    display: table;
    clear: both;
  }
  *zoom: 1;
}
@mixin clearfix() {
  @extend .clearfix;
}

@mixin box-shadow($shadow) {
  -webkit-box-shadow: #{$shadow};
  -moz-box-shadow:    #{$shadow};
  box-shadow:         #{$shadow};
}

@mixin transition($transition:0.1s all) {
  -webkit-transition: #{$transition};
  -moz-transition: #{$transition};
  -o-transition: #{$transition};
  transition: #{$transition};
}

@mixin gradient($from, $to, $fallback) {
  background: $fallback;
  background: -moz-linear-gradient(top, $from 0%, $to 100%);
  background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,$from), color-stop(100%,$to));
  background: -webkit-linear-gradient(top, $from 0%,$to 100%);
  background: -o-linear-gradient(top, $from 0%,$to 100%);
  background: -ms-linear-gradient(top, $from 0%,$to 100%);
  background: linear-gradient(top bottom, $from 0%,$to 100%);
}

@mixin backface($visibility: hidden) {
  -moz-backface-visibility: $visibility;
  -webkit-backface-visibility: $visibility;
  -o-backface-visibility: $visibility;
  backface-visibility: $visibility;
}

// @see http://www.stubbornella.org/content/2010/06/25/the-media-object-saves-hundreds-of-lines-of-code/
@mixin media() {
  overflow: hidden;
  _overflow: visible;
  zoom: 1;
}
  @mixin media-aside($side: left) {
    float: $side;
  }

  @mixin media-body() {
    @include media;
  }

@mixin vertical-align {
  position: relative;
  top: 50%;
  -webkit-transform: translateY(-50%);
  -ms-transform: translateY(-50%);
  transform: translateY(-50%);
}

// ===========================================================================
// Dependency-free breakpoint mixin
// http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
// ===========================================================================
$min: min-width;
$max: max-width;
@mixin at-query ($constraint, $viewport1, $viewport2:null) {
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}

// ===========================================================================
// #Reset
// ===========================================================================
html,body,div,span,applet,object,iframe,h1,h2,h3,h4,h5,h6,p,blockquote,pre,a,abbr,acronym,address,big,cite,code,del,dfn,em,img,ins,kbd,q,s,samp,small,strike,strong,sub,sup,tt,var,b,u,i,center,dl,dt,dd,ol,ul,li,fieldset,form,label,legend,table,caption,tbody,tfoot,thead,tr,th,td,article,aside,canvas,details,embed,figure,figcaption,footer,header,hgroup,menu,nav,output,ruby,section,summary,time,mark,audio,video{border:0;font-size:100%;font:inherit;vertical-align:baseline;margin:0;padding:0;}article,aside,details,figcaption,figure,footer,header,hgroup,menu,nav,section{display:block;}ol,ul{list-style:none;}blockquote,q{quotes:none;}blockquote:before,blockquote:after,q:before,q:after{content:none;}table{border-collapse:collapse;border-spacing:0;}code,pre{background-color:#faf7f5;font-family:Consolas,monospace;border:0 none;padding:0 2px;color:#51ab62;}pre{overflow:auto;padding:0.5em;margin:0 0 1em;}


// ==============================================================================
// #Grid Setup
//
// - Based on csswizardry grid, but with floated columns and a fixed gutter size
// - Breakpoints defined above, under #Breakpoint and Grid Variables
// - Note the inclusion of .grid-uniform to take care of clearfixes on evenly sized grid-items
// ==============================================================================
$responsive:         true;
$mobile-first:       true;
$use-silent-classes: false;
$push:               true;
$pull:               false;

$class-type: unquote(".");

@mixin grid-media-query($media-query) {
  $breakpoint-found: false;

  @each $breakpoint in $breakpoints {
    $name: nth($breakpoint, 1);
    $declaration: nth($breakpoint, 2);

    @if $media-query == $name and $declaration {
      $breakpoint-found: true;

      @media only screen and #{$declaration} {
        @content;
      }
    }
  }

  @if $breakpoint-found == false{
    @warn "Breakpoint '#{$media-query}' does not exist";
  }
}

/*------------------------------------*\
  $HELPERS
\*------------------------------------*/
/**
 * Create helper show/hide classes around our breakpoints.
 */
@mixin device-helper($namespace:"") {

  #{$class-type}#{$namespace}show        { display:block!important; }
  #{$class-type}#{$namespace}hide        { display:none!important; }

  #{$class-type}#{$namespace}text-left   { text-align:left!important; }
  #{$class-type}#{$namespace}text-right  { text-align:right!important; }
  #{$class-type}#{$namespace}text-center { text-align:center!important; }

  #{$class-type}#{$namespace}left        { float:left!important; }
  #{$class-type}#{$namespace}right       { float:right!important; }

}

/* Our regular, non-responsive width and helper classes. */
@include device-helper();

/* Our responsive classes, if we have enabled them. */
@if $responsive == true {

  @each $name in $breakpoint-has-widths {
    @include grid-media-query($name) {
      @include device-helper('#{$name}--');
    }
  }

}


// ===========================================================================
// #Basic Styles
// ===========================================================================
.wrapper {
  @include clearfix();
  max-width: $siteWidth;
  margin: 0 auto;
  padding: 0 .75em;
}

// ===========================================================================
// #Helper Classes
// ===========================================================================
.left { float: left; }
.right { float: right; }
.text-center { text-align: center; }
.text-right { text-align: right; }
.hidden { display: none; }
.hide-text {
  height: 1px;
  width: 1px;
  position: absolute;
  overflow: hidden;
  top: -10px;
}
.display-table {
  display: table;
}

.display-table-cell {
  display: table-cell;
  vertical-align: middle;
}

// ===========================================================================
// #Typography
// ===========================================================================
body,
input,
textarea,
button,
select {
  font-size: 87.5%; // Henseforth known as 1em (assumed at 14px)
  line-height: 1.6;
  font-family: $bodyFontStack;
  font-weight: 300;
  -webkit-font-smoothing: antialiased;
  -webkit-text-size-adjust: 100%;
}

h1, h2, h3, h4, h5, h6 {
  display: block;
  font-family: $headerFontStack;
  font-weight: normal;
  line-height: 1.4;
  a {
    font-weight: inherit;
    text-decoration: none;
  }
}

// Pixel to EM conversion tool http://pxtoem.com/
h1 { font-size: 2.5em; }
h2 { font-size: 2em; }
h3 { font-size: 1.5em; }
h4 { font-size: 1.25em; }
h5 { font-size: 1em; }
h6 { font-size: .9em; }

p { margin: 0 0 1em 0; }
p img { margin: 0; }

em { font-style: italic; }
strong { font-weight: bold; }
small { font-size: 80%; }
sup {
  position: relative;
  top: -0.5em;
  font-size: 60%;
}


// ===========================================================================
// #Lists
// ===========================================================================
ul, ol { margin-bottom: 1em; }
ul { list-style: none outside; }
ol { list-style: decimal; }
ol, ul.square, ul.disc { margin-left: 1em; }
ul.square { list-style: square outside; }
ul.disc { list-style: disc outside; }
ol.alpha { list-style: lower-alpha outside; }
li { margin-bottom: 0.25em; }
.inline-list li {
  display: inline-block;
  margin-bottom: 0;
}


// ===========================================================================
// #Images and Iframes
// ===========================================================================
img.auto,
.grid-item img,
.grid-item iframe {
  max-width: 100%;
  height: auto;
}

img.circle {
  border-radius: 50%;
}


// ===========================================================================
// #Forms
// ===========================================================================
.button,
button,
input[type="text"],
input[type="search"],
input[type="password"],
input[type="email"],
input[type="file"],
input[type="number"],
input[type="tel"],
input[type="submit"],
textarea {
  -webkit-appearance: none;
  -moz-appearance: none;
}

input,
textarea,
select,
fieldset {
  max-width: 100%;
}

input,
select,
textarea {
  font-size: 1em;
  line-height: 1.42;
  margin: 0;
  padding: .2rem;
}

input {
  display: inline-block;
  width: auto;
}

button,
input[type="submit"] {
  cursor: pointer;
}

/* Input width and border */
input[type="text"],
input[type="search"],
input[type="password"],
input[type="email"],
input[type="file"],
input[type="number"],
input[type="tel"],
textarea,
select {
  display: block;
  max-width: 100%;
  width: 100%;
}

textarea {
  min-height: 100px;
}

input[type="checkbox"],
input[type="radio"] {
  display: inline;
  margin: 0;
}

/* Form labels */
label,
legend {
  display: block;
  margin-bottom: 2px;
}

label[for] {
  cursor: pointer;
}

/* Horizontal Form */
form.form-horizontal,
.form-horizontal {
  margin-bottom: 0;

  input[type="text"],
  input[type="search"],
  input[type="password"],
  input[type="email"],
  input[type="file"],
  input[type="number"],
  input[type="tel"],
  textarea,
  select,
  label {
    display: inline-block;
    margin-bottom: 0;
    width: auto;
  }
}

// Forms
input[type="text"],
input[type="search"],
input[type="password"],
input[type="email"],
input[type="file"],
input[type="number"],
input[type="tel"],
textarea,
select {
  background-color: #fff;
  border: 1px solid scale-color($body-color, $lightness: +50%);
}

legend {
  color: #444;
}

.field-group {
  margin-bottom: .5em;
}
  .field-group__legend {
    font-size: 1.5em;
    font-weight: 700;
    margin-bottom: .5em;
  }

.input,
.select,
.textarea {
  margin: 0 0 .5em 0;
}
  .input-hint {
    padding: .15em .4em;
    border-radius: 5px;
  }
  .input-hint--info {
    background: #68708A;
    border-radius: 5px;
    color: #fff;
  }
  .input-hint--error {
    background: #DF5900;
    border-radius: 5px;
    color: #fff;
  }

.submit {
  text-align: center;
}

// Messages
.message {
  margin-bottom: 1em;
  padding: .5em 1em;
}
  .message__description {
    p {
      margin-bottom: 0;
    }
  }
.message--success {
  background-color: #4E6F25;
  color: #fff;
  border-radius: 5px;
}
.message--notice {
  background-color: #DF5900;
  color: #fff;
  border-radius: 5px;
}
.message--error {
  background-color: #811515;
  color: #fff;
  border-radius: 5px;
}


// ===========================================================================
// #Links and Buttons
// ===========================================================================
.button {
  background-color: $accent-color;
  border: 0;
  color: $font-color-on-accent-background;
  cursor: pointer;
  display: inline-block;
  margin: 0;
  padding: .5em 2em;
  text-align: center;
  text-decoration: none;
  vertical-align: middle;
  white-space: nowrap;
  -webkit-appearance: none;
  transition: background-color .25s;

  &.button--small {
    font-size: 0.75em;
    padding: .25em .5em;
  }

  &.button--full {
    width: 100%;
  }

  &:hover,
  &:active,
  &:focus {
    background-color: scale-color($accent-color, $lightness: -15%);
  }

  &[disabled],
  &.disabled {
    cursor: default;
  }
}


// ===========================================================================
// #Layout
// ===========================================================================
body {
  color: $body-color;
  background-color: $body-background;
}
.clearer {
  clear: both;
}
ul, li {
  margin-bottom: 0;
}
p {
  margin-bottom: .25em;
}
a {
  color: $accent-color;
  text-decoration: none;
}
.page {
  @extend .clearfix;
}

.main-header {
  overflow: hidden;
}
  .logo {
    float: left;
    margin-right: .5em;
  }
    .logo__link {
      text-decoration: none;
    }
      .logo__image {
        padding: .25em;
        vertical-align: middle;
        height: 50px;
      }

.restaurant-name {
  float: left;
  {% unless restaurant.logo? %}
    margin-left: .5em;
  {% endunless %}
  {% unless restaurant.slogan? %}
    margin-top: .5em;
  {% endunless %}
}
  .slogan {
    color: $body-color;
    font-size: .9em;
    font-style: italic;
  }
  .restaurant-name__link {
    color: $body-color;
    font-family: $headerFontStack;
    text-decoration: none;
    font-size: 1.25em;
  }

.phone-large {
  display: none;
}

.main-header__hamburger {
  float: right;
  margin-right: 5px;
  cursor: pointer;
  margin-top: 8px;
}
.main-header__hamburger-stroke {
  div {
    margin-bottom: .2em;
    background-color: $body-color;
    height: 3px;
    width: 42px;
    border-radius: 5px;
  }
}
.main-header__hamburger-title {
  font-size: 1em;
  text-transform: uppercase;
  font-weight: bold;
  color: $body-color;
  margin-bottom: 0;
}

.phone-medium-down {
  text-align: center;
  margin: .5em 0;
}
  .phone-number {
  }
    .phone-number__link {
      color: $accent-color;
      font-size: 1.5em;
      text-decoration: none;
      &:hover, &:focus {
        .phone-number__label {
          color: $accent-color;
        }
      }
    }
      .phone-number__label {
        color: $body-color;
        font-weight: bold;
        transition: color .25s;
      }

.main-nav {
  display: none;
  position: absolute;
  z-index: 99;
  right: 1em;
  top: 4em;
  border-radius: 5px;
  background-color: $nav-background;
}
  .main-nav__triangle {
    top: -1em;
    right: 0;
    position: absolute;
    width: 0;
    height: 0;
    border-style: solid;
    border-width: 0 20px 20px 20px;
    border-color: transparent transparent $nav-background transparent;
  }
    .main-nav__items {
      padding: 1em 2.5em;
    }
      .main-nav__item {
        margin: .5em 0;
      }
      .main-nav__link {
        text-decoration: none;
        color: $nav-font-color;
        font-size: 1.5em;
        transition: color .25s;
        &:hover {
          color: $accent-color;
        }
      }

.main-content {
  padding-bottom: 5.5em;
}

.main-footer {
}
  .footer__social {
    position: fixed;
    bottom: 0;
    width: 100%;
    background-color: $card-background;
    border-top: 4px solid $accent-color;
  }
    .social__items {
      text-align: center;
    }
      .social__item {
        margin: 0 .5em;
        display: inline-block;
      }
      .social__item--newsletter {
        display: none;
      }
        .social__link {
          color: $card-font-color;
          padding: .5em .25em;
          font-size: 2em;
          text-decoration: none;
          vertical-align: bottom;
          transition: color .25s;
          display: block;
          &:hover, &:focus {
            color: $accent-color;
          }
        }
          .social__label {
            @extend .hide-text;
          }

  .footer__legal {
    margin-bottom: 1em;
    text-align: center;
  }
    .legal__link {
      color: scale-color($body-color, $lightness: +25%);
      text-decoration: none;
      transition: color .25s;
      &:hover, &:focus {
        color: $body-color;
      }
    }


/**
 * masonry elements styles
 */
.item {
  width: 48%;
  background-color: $card-background;
  margin: 1%;
  text-align: center;
  transition: background-color .25s;
  border-radius: 5px;
  &:hover {
    background-color: $card-background-hover;
  }
}
.item-wrapper-image {
  position: relative;
  width: 100%;
  height: 0;
  padding-bottom: 54%;
}
.item-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: block;
}
.item.see-all-menus {
  background-color: $accent-color;
}
  .see-all-menus {
    cursor: pointer;
    transition: background-color .25s;
    a {
      color: $font-color-on-accent-background;
      text-decoration: none;
      font-size: 1.25em;
      font-weight: bold;
      display: block;
      margin-bottom: .5em;
    }
    &:hover {
      background-color: $card-background-hover;
    }
    img {
      border-top-left-radius: 5px;
      border-top-right-radius: 5px;
      margin-bottom: .5em;
    }
    .menu__title {
      margin-top: .25em;
      font-size: 1em;
    }
  }

// ===========================================================================
// #Modules
// ===========================================================================

.page-header__header {
}
.page-header__title {
  text-align: center;
}
.page-header__description {
  text-align: center;
  padding: 0 .75em;
  color: $body-color;
}
// Default page
.page-header {
  padding: 0;
}
.page-header--page {
  margin: 1.5em 0;
}
.page-body {
  ol, ul, dl {
    margin-left: 2em;
  }
  ul {
    list-style: disc;
  }
  img {
    max-width: 100% !important;
    height: auto !important;
  }
  a {
    color: $accent-color;
    text-decoration: none;
    &:hover, &:focus {
      color: scale-color($accent-color, $lightness:  -25%);
    }
  }
}

// block
.block {
  padding: 0 .25em;
  margin-bottom: 1em;
}
.block__body {
  border-radius: 5px;
  background-color: $card-background;
  color: $card-font-color;
  text-align: center;
}
.block__header {
  text-align: center;
  background-color: $accent-color;
  border-top-left-radius: 5px;
  border-top-right-radius: 5px;
  .section__title {
    padding: 1em 0;
    color: $font-color-on-accent-background;
  }
}
.block__image {
  background-size: cover;
  background-position: center center;
  height: 200px;
  border-bottom-left-radius: 5px;
  border-bottom-right-radius: 5px;
}

// shortcut
.shortcut-wrapper {
  text-align: center;
  margin: 0 0 .5em 0;
}
.shortcut {
  position: relative;
  display: inline-block;
  margin: .5em .25em;
  width: 80px;
  height: 80px;
  border-radius: 5px;
  background-color: $accent-color;
  &:hover .shortcut__overlay {
    opacity: .25;
  }
}
.shortcut__overlay {
  position: absolute;
  border-radius: 5px;
  top: 0; left: 0; right: 0; bottom: 0;
  background-color: $card-background;
  opacity: 0.5;
  transition: opacity .25s;
}
.shortcut__link {
  text-decoration: none;
  color: $font-color-on-photo;
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
}
.shortcut__header {
  text-align: center;
  position: absolute;
  bottom: .25em;
  left: 0;
  right: 0;
  .section__title {
    font-size: 1em;
    font-family: $bodyFontStack;
    font-weight: bold;
  }
}

// Home page

.featured {
  margin-bottom: .5em;
}

.menus__footer,
.foods__footer {
  text-align: center;
}

.opening-hours {
  margin-bottom: 1em;
  {% if theme_settings.home-image-opening-hours %}
    margin-bottom: 0;
  {% endif %}
}
.opening-hours__image {
  background-image: url({{ 'home-image-opening-hours.jpg' | asset_url }});
  margin-bottom: 1em;
}

.opening-hours__body {
  {% if theme_settings.home-image-opening-hours %}
    border-bottom-left-radius: 0px;
    border-bottom-right-radius: 0px;
  {% endif %}
}
  .opening-days {
    padding: 1em;
  }
    .opening-day {
      color: $accent-color;
      font-size: 1.25em;
    }
    .opening-shifts {
      color: $card-font-color;
      font-size: 1em;
      margin-bottom: .5em;
    }
    .opening-hours__information {
      padding: 1em;
      color: $card-font-color;
    }

.directions__image {
  background-image: url({{ 'home-image-directions.jpg' | asset_url }});
}
.directions__body {
}
  .map {
    position: relative;
  }
  .map-overlay {
    position: absolute;
    background-color: transparentize($card-background, 0.75);
    top: 0; left: 0; right: 0; bottom: 0;
  }
    .map__image {
      width: 100%;
      height: auto;
    }
  .directions__footer,
  .business-view__footer {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background-color: $card-background;
    text-align: center;
    border-bottom-left-radius: 5px;
    border-bottom-right-radius: 5px;
    {% if theme_settings.home-image-directions %}
      border-bottom-left-radius: 0px;
      border-bottom-right-radius: 0px;
    {% endif %}
    a {
      background-color: $card-background;
      width: 100%;
      border-bottom-left-radius: 5px;
      border-bottom-right-radius: 5px;
      {% if theme_settings.home-image-directions %}
        border-bottom-left-radius: 0px;
        border-bottom-right-radius: 0px;
      {% endif %}
    }
  }

// all menus - menus template
.breadcrumb {
  text-align: center;
  width: 100%;
}
// Menu - menu template
.menu {
  padding-bottom: 1em;
}
  .menu--featured {
  }
  .item-tag {
    color: $font-color-on-accent-background;
    text-align: center;
    padding: .25em 0;
    font-weight: bold;
    font-family: $bodyFontStack;
    background-color: $accent-color;
    margin-bottom: .5em;
  }
  .item-tag--no-img {
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
  }
  .featured__menu-image {
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
  }
  .menu__header {
  }
    .menu__title {
      color: $card-font-color;
      margin-bottom: .5em;
      padding: 0 .5em;
      font-family: $bodyFontStack;
      font-size: 1.25em;
      font-weight: 700;
    }
  .menu__description {
    font-size: 1em;
    margin-bottom: 1em;
  }

// Food - food template
.food {
}
  .food__image {
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
  }
  .food__body {
    margin: .5em 0;
    padding: 0 .5em;
  }
    .food__title {
      color: $card-font-color;
      font-family: $bodyFontStack;
      font-size: 1.25em;
      font-weight: 700;
      margin-bottom: .25em;
    }
    .food__summary {
      font-size: 1em;
      color: $summary-color;
      line-height: 1.35;
    }
.food--featured {
  .icon-star {
    color: $accent-color;
  }
}

// Food prices
.food__prices {
  margin-top: 1em;
}
.prices {
}
  .price {
    @include clearfix;
  }
  .price__label {
    color: $card-font-color;
    float: left;
  }
  .price__value {
    font-weight: 700;
    float: right;
    color: $food-price-value-color;
  }

// Tags
.food__tags {
}
  .tag {
    font-size: .95em;
    border-radius: $radius;
    padding: .2em .4em;
  }
    .tag--vegan, .tag--vegetarian {
      background-color: $tag-vegetarian-background;
      color: $tag-vegetarian-font-color;
    }
    .tag--new {
      background-color: $tag-news-background;
      color: $tag-news-font-color;
    }
    .tag--no-meat {
      background-color: $tag-no-meat-background;
      color: $tag-no-meat-font-color;
    }
    .tag--no-pork {
      background-color: $tag-no-pork-background;
      color: $tag-no-pork-font-color;
    }
    .tag--alcohol-free {
      background-color: $tag-no-alcohol-background;
      color: $tag-no-alcohol-font-color;
    }
    .tag--daily-food {
      background-color: $tag-daily-food-background;
      color: $tag-daily-food-font-color;
    }

// Gallery
.page-header--restaurant {
  margin-bottom: 1em;
}
.restaurant-gallery {
  margin-bottom: 1em;
}
  .gallery__img {
    img {
      width: 100%;
      border-top-left-radius: 5px;
      border-top-right-radius: 5px;
    }
  }
  .photo__link {
    text-decoration: none;
    display: block;
    margin-bottom: .25em;
    &>div.item-wrapper-image {
      padding-bottom: 58%;
    }
  }
  .photo__caption {
    color: $card-font-color;
  }

/*
 * Venobox css
 */
.vbox-overlay *, .vbox-overlay *:before, .vbox-overlay *:after{
    -webkit-backface-visibility: hidden;
    -webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box;}
body{
    -webkit-overflow-scrolling:touch;
}
/* ------- overlay: change here background color and opacity ----- */
.vbox-overlay{
    background: #181818;
    background: rgba(0,0,0,0.85);
    width: 100%;
    height: auto;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 999;
    overflow: hidden;
    opacity: 0;
    transition: opacity .25s ease-in-out;
    -moz-transition: opacity .25s ease-in-out;
    -webkit-transition: opacity .25s ease-in-out;
}
.relativo{
    float: left;
    width: 100%;
    height: 100%;
    display: block;
    position: relative;
}
/* ----- preloader - choose between CIRCLE, IOS, DOTS, QUADS ----- */

/* circle preloader */
.vbox-preloader{
    position:fixed;
    width:32px;
    height:32px;
    left:50%;
    top:50%;
    margin-left:-16px;
    margin-top:-16px;
    background-image: url("{{ 'preload-circle.png | asset_url '}}");
    text-indent: -100px;
    overflow: hidden;
    -webkit-animation: playload 1.4s steps(18) infinite;
       -moz-animation: playload 1.4s steps(18) infinite;
        -ms-animation: playload 1.4s steps(18) infinite;
         -o-animation: playload 1.4s steps(18) infinite;
            animation: playload 1.4s steps(18) infinite;
}
@-webkit-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -576px; }
}
@-moz-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -576px; }
}
@-ms-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -576px; }
}
@-o-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -576px; }
}
@keyframes playload {
   from { background-position:    0px; }
     to { background-position: -576px; }
}
/* IOS preloader */

.vbox-preloader{
    position:fixed;
    width:32px;
    height:32px;
    left:50%;
    top:50%;
    margin-left:-16px;
    margin-top:-16px;
    background-image: url("{{ 'preload-ios.png' | asset_url }}");
    text-indent: -100px;
    overflow: hidden;
    -webkit-animation: playload 1.4s steps(12) infinite;
       -moz-animation: playload 1.4s steps(12) infinite;
        -ms-animation: playload 1.4s steps(12) infinite;
         -o-animation: playload 1.4s steps(12) infinite;
            animation: playload 1.4s steps(12) infinite;
}

@-webkit-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@-moz-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@-ms-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@-o-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}

/* dots preloader */

.vbox-preloader{
    position:fixed;
    width:32px;
    height:11px;
    left:50%;
    top:50%;
    margin-left:-16px;
    margin-top:-16px;
    background-image: url("{{ 'preload-dots.png' | asset_url }}");
    text-indent: -100px;
    overflow: hidden;
    -webkit-animation: playload 1.4s steps(24) infinite;
       -moz-animation: playload 1.4s steps(24) infinite;
        -ms-animation: playload 1.4s steps(24) infinite;
         -o-animation: playload 1.4s steps(24) infinite;
            animation: playload 1.4s steps(24) infinite;
}

@-webkit-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -768px; }
}
@-moz-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -768px; }
}
@-ms-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -768px; }
}
@-o-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -768px; }
}
@keyframes playload {
   from { background-position:    0px; }
     to { background-position: -768px; }
}

/* quads preloader */

.vbox-preloader{
    position:fixed;
    width:32px;
    height:10px;
    left:50%;
    top:50%;
    margin-left:-16px;
    margin-top:-16px;
    background-image: url("{{ 'preload-quads.png' | asset_url }}");
    text-indent: -100px;
    overflow: hidden;
    -webkit-animation: playload 1.4s steps(12) infinite;
       -moz-animation: playload 1.4s steps(12) infinite;
        -ms-animation: playload 1.4s steps(12) infinite;
         -o-animation: playload 1.4s steps(12) infinite;
            animation: playload 1.4s steps(12) infinite;
}
@-webkit-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@-moz-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@-ms-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@-o-keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}
@keyframes playload {
   from { background-position:    0px; }
     to { background-position: -384px; }
}

/* ----- navigation ----- */
.vbox-close{
    cursor: pointer;
    position: fixed;
    top: -1px;
    right: 0;
    width: 46px;
    height: 40px;
    padding: 10px 20px 10px 0;
    display: block;
    background: url("{{ 'close.gif' | asset_url }}") no-repeat #161617;
    background-position:10px center;
    color: #fff;
    text-indent: -100px;
    overflow: hidden;
}
.vbox-next, .vbox-prev{
    box-sizing: content-box;
    cursor: pointer;
    position: fixed;
    top: 50%;
    color: #fff;
    width: 30px;
    height: 170px;
    margin-top: -85px;
    text-indent: -100px;
    border: solid transparent; /* Using border instead of padding to keep bg image in place */
    overflow: hidden;
}
.vbox-prev{
    left: 0;
    border-width: 0 30px 0 10px;
    background: url("{{ 'prev.gif' | asset_url }}") center center no-repeat;
}
.vbox-next{
    right: 0;
    border-width: 0 10px 0 30px;
    background: url("{{ 'next.gif' | asset_url }}") center center no-repeat;
}

.vbox-title{
    width: 100%;
    height: 40px;
    float: left;
    text-align: center;
    line-height: 28px;
    font-size: 12px;
    color: #fff;
    padding: 6px 40px;
    overflow: hidden;
    background: #161617;
    position: fixed;
    display: none;
    top: -1px;
    left: 0;

}
.vbox-num{
    cursor: pointer;
    position: fixed;
    top: -1px;
    left: 0;
    height: 40px;
    display: block;
    color: #fff;
    overflow: hidden;
    line-height: 28px;
    font-size: 12px;
    padding: 6px 10px;
    background: #161617;
    display: none;
}
/* ------- inline window ------ */
.vbox-inline{
    width: 420px;
    height: 315px;
    padding: 10px;
    background: #fff;
    text-align: left;
    margin: 0 auto;
    overflow: auto;
}

/* ------- Video & iFrames window ------ */
.venoframe{
    border: none;
    width: 960px;
    height: 720px;
}
@media (max-width: 992px) {
    .venoframe{
        width: 640px;
        height: 480px;
    }
}
@media (max-width: 767px) {
    .venoframe{
        width: 420px;
        height: 315px;
    }
}
@media (max-width: 460px) {
    .vbox-inline{
        width: 100%;
    }

    .venoframe{
        width: 100%;
        height: 260px;
    }
}

/* ------- PLease do NOT edit this! (or do it at your own risk) ------ */
.vbox-container{
    position: relative;
    background: #000;
    width: 98%;
    max-width: 1024px;
    margin: 0 auto;
}
.vbox-content{
    text-align: center;
    float: left;
    width: 100%;
    position: relative;
    overflow: hidden;
}
.vbox-container img{
    max-width: 100%;
    height: auto;
}
.vwrap{
    opacity: 1;
    transition: opacity .25s ease-in-out;
    -moz-transition: opacity .25s ease-in-out;
    -webkit-transition: opacity .25s ease-in-out;
    width: 100%;
    height: 100%;
    float: left;
    position: relative;
    margin: 0;
    padding: 0;
    left: 0;
    overflow: hidden;
    z-index: 1;
}

// Contact page
.page-body--contact {
  margin: 1.5em 0;
  padding: 0 .25em;
  input {
    margin-bottom: 1.5em;
  }
  input, textarea {
    outline: none;
    border-radius: 5px;
  }
  textarea {
    border-bottom-left-radius: 0px;
    border-bottom-right-radius: 0px;
  }
}
.textarea--message {
  margin-bottom: 0;
}
input.submit-contact {
  width: 100%;
  margin-bottom: 0;
  border-top-left-radius: 0px;
  border-top-right-radius: 0px;
}
.contact-item {
  a {
    color: $accent-color;
    text-decoration: none;
    &:hover, &:focus {
      color: scale-color($accent-color, $lightness: -25%);
    }
  }
}
  .contact-item__name {
    color: $card-font-color;
    margin-bottom: .5em;
  }
  .contact-item__label {
    font-weight: 700;
  }
  .aside--contact {
    .block__body {
      border-radius: 5px;
    }
  }
  .restaurant-informations__content {
    padding: 1em;
  }

// newsletter
.newsletter-form {
  margin: 0 auto 2em auto;
  .input {
    margin-bottom: 0;
    input {
    outline: none;
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
    }
  }
  .submit-newsletter {
    width: 100%;
    border-bottom-left-radius: 5px;
    border-bottom-right-radius: 5px;
  }
}
.section__subscribe--home {
}
.section__subscribe--layout {
  display: none;
}

.section__subscribe {
  max-width: 300px;
  margin: .5em auto 1em auto;
  .input {
    margin-bottom: 0;
    input {
    outline: none;
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
    }
  }
  .submit-newsletter {
    width: 100%;
    border-bottom-left-radius: 5px;
    border-bottom-right-radius: 5px;
  }
}

// Page
.page-body--page {
  img {
    border-radius: 5px;
  }
}

// All News
.page-header--news {
  margin-bottom: 1.5em;
  text-align: left;
}
.news {
  margin-top: 1em;
}
.news__body {
  text-align: left;
}
.news__header {
  transition: background-color .25s;
  &:hover {
    background-color: scale-color($accent-color, $lightness: -25%);
  }
}
.home-news-title__link {
  text-decoration: none;
  color: $font-color-on-accent-background;
}
.news__one-news {
  background-color: $card-background;
  transition: background-color .25s;
  border-bottom: 8px solid $accent-color;
  &:hover {
    background-color: $card-background-hover;
  }
  .block__image {
    border-radius: 0px;
  }
}
.news__one-news--last {
  border-bottom-left-radius: 5px;
  border-bottom-right-radius: 5px;
}
.news__one-news--home-last {
  {% if theme_settings.home-image-news %}
    border-bottom-left-radius: 0px;
    border-bottom-right-radius: 0px;
  {% endif %}
}
.news__one-news-link {
  text-decoration: none;
  display: block;
}

.news__one-news-content-wrapper {
  padding: 1.5em 2em;
}
.news__one-news-content {
}
.item-tag--date {
  //background-color: scale-color($card-background-hover, $lightness: -15%);
  background-color: #fff;
  color: $body-color;
}
.news__one-news-title {
  color: $card-font-color;
  font-family: $bodyFontStack;
  font-size: 1.25em;
  font-weight: 700;
  margin-bottom: .25em;
}
.news__one-news-summary {
  font-size: 1em;
  color: $summary-color;
  line-height: 1.35;
}
.news__image {
  background-image: url({{ 'home-image-news.jpg' | asset_url }});
}
// News
.page-body--news {
  img {
    border-radius: 5px;
  }
}
.news__one-news--first {
  border-top-right-radius: 5px;
  border-top-left-radius: 5px;
}
.news__date-wrapper {
}
.news__date {
  font-family: $bodyFontStack;
  font-weight: bold;
  color: $body-color;
  margin-top: .5em;
}

/*
  Icon Font: fontcustom
*/

@font-face {
  font-family: "fontcustom";
  src: url("./fontcustom_c35105239c362af506f42e5466fe6044.eot");
  src: url("./fontcustom_c35105239c362af506f42e5466fe6044.eot?#iefix") format("embedded-opentype"),
       url("./fontcustom_c35105239c362af506f42e5466fe6044.woff") format("woff"),
       url("./fontcustom_c35105239c362af506f42e5466fe6044.ttf") format("truetype"),
       url("./fontcustom_c35105239c362af506f42e5466fe6044.svg#fontcustom") format("svg");
  font-weight: normal;
  font-style: normal;
}

@media screen and (-webkit-min-device-pixel-ratio:0) {
  @font-face {
    font-family: "fontcustom";
    src: url("./fontcustom_c35105239c362af506f42e5466fe6044.svg#fontcustom") format("svg");
  }
}

[data-icon]:before { content: attr(data-icon); }

[data-icon]:before,
.icon-arrow-left:before,
.icon-arrow-right:before,
.icon-check:before,
.icon-close:before,
.icon-envelop:before,
.icon-error:before,
.icon-facebook:before,
.icon-newsletter:before,
.icon-phone:before,
.icon-star:before,
.icon-tripadvisor:before,
.icon-twitter:before {
  display: inline-block;
  font-family: "fontcustom";
  font-style: normal;
  font-weight: normal;
  font-variant: normal;
  line-height: 1;
  text-decoration: inherit;
  text-rendering: optimizeLegibility;
  text-transform: none;
  -moz-osx-font-smoothing: grayscale;
  -webkit-font-smoothing: antialiased;
  font-smoothing: antialiased;
}

.icon-arrow-left:before { content: "\f100"; }
.icon-arrow-right:before { content: "\f101"; }
.icon-check:before { content: "\f102"; }
.icon-close:before { content: "\f103"; }
.icon-envelop:before { content: "\f104"; }
.icon-error:before { content: "\f105"; }
.icon-facebook:before { content: "\f106"; }
.icon-newsletter:before { content: "\f10b"; }
.icon-phone:before { content: "\f107"; }
.icon-star:before { content: "\f108"; }
.icon-tripadvisor:before { content: "\f109"; }
.icon-twitter:before { content: "\f10a"; }


// ===========================================================================
// #Breakpoints
// ===========================================================================

// Medium
@include at-query($min, $small) {
  body,
  input,
  textarea,
  select {
    font-size: 105%;
  }
  .logo__image {
    height: 60px;
  }
  .main-header__hamburger {
    margin-top: 0;
  }
  .main-header__hamburger-stroke {
    margin-top: 5px;
    display: inline-block;
    vertical-align: middle;
    div {
      width: 25px;
      height: 4px;
    }
  }
  .main-header__hamburger-title {
    display: inline-block;
    vertical-align: middle;
    font-size: 1.5em;
  }
  .phone {
    margin: 1em 0;
  }
  .main-nav {
    top: 3em;
  }
  .block__image {
    height: 250px;
  }
  // shortcut
  .shortcut {
    width: 100px;
    height: 100px;
  }

  /**
   * masonry elements styles
   */
  #container {
    margin: 1em;
  }
  .item {
    width: 31.333333%;
    margin: 1%;
  }
}

// Large
@include at-query($min, $medium) {
  .main-header {
  }
  .logo__image {
    height: 65px;
  }
  .restaurant-name {
    {% unless restaurant.logo? %}
      margin-left: 1.5em;
    {% endunless %}
  }
  .phone-large {
    display: block;
    float: left;
    margin-left: 2em;
    margin-top: .5em;
  }
  .phone-medium-down {
    display: none;
  }
  .footer__social {
    position: fixed;
    right: .5em;
    bottom: 4em;
    width: 8%;
    background-color: transparent;
    border-top: none;
  }
    .social__items {
      text-align: center;
    }
      .social__item {
        width: 50px;
        height: 50px;
        background-color: $card-background;
        border-radius: 50%;
        margin: .5em 0;
        transition: background-color .25s;
        &:hover {
          background-color: $accent-color;
        }
      }
      .social__item--phone {
        display: none;
      }
        .social__link {
          font-size: 1.35em;
          padding: .25em;
          &:hover {
            color: $card-font-color;
          }
        }
  .main-content {
    overflow: hidden;
    padding: 0 5em 1.5em 1em;
  }
  // block
  .block {
    overflow: hidden;
    position: relative;
    padding: 0;
    margin: 0 .5% 1em;
  }
  .block__image {
    position: absolute;
    width: 50%;
    height: auto;
    margin-bottom: 0;
  }

  .opening-hours {
    {% if theme_settings.home-image-opening-hours %}
      width: 99%;
    {% else %}
      width: 49%;
      float: left;
    {% endif %}

    {% if theme_settings.home-image-directions %}
      width: 99%;
    {% endif %}
  }
  .opening-hours__body {
    {% if theme_settings.home-image-opening-hours %}
      width: 50%;
      float: left;
    {% else %}
      width: 100%;
      border-bottom-right-radius: 5px;
    {% endif %}
    border-bottom-left-radius: 5px;
    margin: 0;
  }
  .opening-hours__header {
    {% if theme_settings.home-image-opening-hours %}
      border-top-right-radius: 0;
    {% endif %}
  }
  .opening-hours__image {
    top: 0; left: 50%; right: 0; bottom: 0;
    border-top-right-radius: 5px;
    border-bottom-left-radius: 0;
  }

  .directions {
    {% if theme_settings.home-image-directions %}
      width: 99%;
    {% else %}
      width: 49%;
      float: right;
    {% endif %}

    {% if theme_settings.home-image-opening-hours %}
      width: 99%;
    {% endif %}
  }
  .directions__body {
    {% if theme_settings.home-image-directions %}
      width: 50%;
      float: right;
    {% else %}
      width: 100%;
    {% endif %}
    margin: 0;
  }
  .directions__header {
    {% if theme_settings.home-image-directions %}
      border-top-left-radius: 0;
    {% endif %}
  }
  .directions__image {
    top: 0; left: 0; right: 50%; bottom: 0;
    border-top-left-radius: 5px;
    border-bottom-right-radius: 0;
  }
  .directions__footer {
    border-bottom-right-radius: 5px;
    {% unless theme_settings.home-image-directions %}
      border-bottom-left-radius: 5px;
    {% endunless %}
    a {
      border-bottom-right-radius: 5px;
      {% unless theme_settings.home-image-directions %}
        border-bottom-left-radius: 5px;
      {% endunless %}
    }
  }
  // shortcut
  .shortcut {
    width: 120px;
    height: 120px;
  }

  .section__subscribe {
    max-width: 500px;
    .input {
      margin-bottom: 0;
      float:left;
      width: 75%;
      input {
        border-top-right-radius: 0px;
        border-bottom-left-radius: 5px;
      }
    }
    .submit {
      width: 25%;
      float: right;
    }
    .submit-newsletter {
      height: 2.3em;
      border-bottom-left-radius: 0px;
      border-top-right-radius: 5px;
    }
  }
  .lb-overlay a.lb-close{
    top: 45px;
  }
  .lb-overlay:target {
    width: auto;
    padding: 80px 100px 120px 100px;
  }

  // contact
  .aside--contact,
  .page-header--contact,
  .page-body--contact {
    padding: 0;
  }

  .aside--contact {
    .block {
      float: left;
      width: 48%;
      margin: 1%;
    }
    .block__body {
      width: 100%;
      margin: 0;
    }
    .block__header {
      border-radius: 5px;
    }
  }

  // news
  .news__one-news--home-last {
    //{% if theme_settings.home-image-news %}
      border-bottom-left-radius: 5px;
      border-bottom-right-radius: 5px;
    //{% endif %}
  }
  .news__one-news {
    overflow: hidden;
    position: relative;
  }
  .news__one-news--odd {
    .block__image {
      top: 0; left: 0; right: 50%; bottom: 0;
    }
    .news__one-news-content-wrapper {
      float: right;
    }
  }
  .news__one-news--even {
    .block__image {
      top: 0; left: 50%; right: 0; bottom: 0;
    }
    .news__one-news-content-wrapper {
      float: left;
    }
  }
  .news__one-news-content-wrapper {
    width: 50%;
  }
  .news__one-news-content-wrapper--no-image {
    width: 100%;
    float: none;
  }
  .news__image {
    display: none;
  }
  /**
   * masonry elements styles
   */
  .item {
    width: 23%;
    margin: 1%;
  }
  .menus__body {
    .item {
      width: 31.333333%;
      margin: 1%;
    }
  }
}

// > Large
@include at-query($min, $siteWidth) {
  .footer__social {
    width: 6%;
  }
  /**
   * masonry elements styles
   */
  .item {
    width: 19%;
    margin: 0.5%;
  }
}

// >Desktop
@include at-query($min, 1025px) {
  .social__item--newsletter {
    display: inline-block;
    cursor: pointer;
  }
  // newsletter
  .section__subscribe--home {
    display: none;
  }
  .section__subscribe--layout {
    width: 350px;
    background-color: #FFF;
    border-radius: 5px;
    padding: 1.5em;
    position: absolute;
    top: 1.75em;
    left: -21em;
    .input--layout, .submit--layout {
      float: none;
      width: 100%;
    }
  }
  .section__subscribe-triangle {
    position: absolute;
    top: 2.5em;
    right: -1em;
    width: 0;
    height: 0;
    border-style: solid;
    border-width: 20px 0 20px 20px;
    border-color: transparent transparent transparent #FFFFFF;
  }
}

// >xlarge
@include at-query($min, 1200px) {
  .news__body--home {
    {% if theme_settings.home-image-news %}
      width: 50%;
      float: right;
      border-top-left-radius: 0px;
      border-bottom-left-radius: 0px;
    {% else %}
      width: 100%;
    {% endif %}
    .news__header, .news__one-news--last {
      {% if theme_settings.home-image-news %}
        border-top-left-radius: 0px;
        border-bottom-left-radius: 0px;
      {% endif %}
    }
  }
  .news__image {
    display: block;
    top: 0; left: 0; right: 50%; bottom: 0;
    border-top-left-radius: 5px;
    border-bottom-left-radius: 5px;
    border-top-right-radius: 0px;
    border-bottom-right-radius: 0px;
  }
}

// > XLarge
@include at-query($min, 1400px) {
  .footer__social {
    width: 4%;
  }
}